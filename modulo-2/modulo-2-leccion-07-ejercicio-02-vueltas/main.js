'use strict'

let acc = 0;

for (let i = 0; i < 10; i++) {
    acc += 2;
  }
  
console.log(`El resultado es: ` + acc + `, siendo ` + acc + ` el valor de la variable acc`);

for (let acc = 0; acc < 20; acc+=2) {
  
console.log(`El resultado es: ` + acc + `, siendo ` + acc+ ` el valor de la variable acc`);
}